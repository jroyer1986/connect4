import { Space } from '../shared/models/space';

export class Helpers{
	public static isSpacePlayable(space: Space){
		//logic here decides if space is available for play
		let isPlayable: boolean = true;
		
		return isPlayable;
	}
}